# Golden question-and-reply fixture: discuss.python.org topic 107565

## Status

Reviewed question-and-reply fixture for `PRODUCT-INV-PIPELINE-001`.

This directory is investigation evidence.
Its file layout and field choices do not define the normative learning-unit package schema.

## Source sequence and relationships

Topic:

`Thoughts on standardizing build backend support for non-standard targets`

Source:

`https://discuss.python.org/t/107565`

Selected source sequence:

`#1 MattP -> #2 pf_moore -> #4 MattP -> #7 cjames23`

The arrow above expresses selected source-sequence order. It does not claim that every edge is an explicit Discourse reply.

Source-native relationships:

| post | API `reply_to_post_number` |
|---|---:|
| `#1` | `null` |
| `#2` | `null` |
| `#4` | `2` |
| `#7` | `4` |

Post `#2` is a topic-level response. The earlier tree projection attached topic-level responses to root post `#1` for traversal. That projected parent is not source-native reply metadata.

## Interaction coverage in this fixture

| post | coverage |
|---|---|
| `#1` | Question-formulation interaction generated and reviewed. |
| `#2` | Reply-formulation interaction generated and reviewed. |
| `#4` | Retained in the selected source sequence; quiz generation not investigated. |
| `#7` | Retained in the selected source sequence; quiz generation not investigated. |

## Reviewed interaction order

1. Show `question-interaction.json`.
2. Reveal authentic post `#1` from `source-posts.json`.
3. Show `reply-interaction.json` as a response to the accepted generated question from interaction 1.
4. Reveal authentic post `#2` from `source-posts.json`.

Posts `#4` and `#7` are source-sequence evidence only in this fixture.
The fixture does not establish that selected source turns may omit quizzes.

## Files

| file | purpose |
|---|---|
| `source-topic.raw.json` | Untouched HTTP response body from the Discourse topic endpoint. |
| `source-capture.json` | Capture timestamp, request metadata, SHA-256 digest, and phrase checks. |
| `source-posts.json` | API-derived selected posts with source-native reply metadata and exact raw text. |
| `source-posts.pre-api-normalized.json` | Superseded pre-capture fixture retained as comparison evidence. |
| `source-comparison.md` | Comparison between the pre-capture fixture and API-derived posts. |
| `question-interaction.json` | Reviewed question-formulation interaction generated by `gpt-oss-20b`. |
| `reply-interaction.json` | Reviewed reply-formulation interaction generated by `gpt-oss-20b`. |
| `review.md` | Human review verdicts, generation evidence, and comparison observations. |

## Boundaries demonstrated by this fixture

- Generated learner-facing content remains separate from authentic source text.
- Each interaction carries one exact authentic source phrase.
- The session contains both question-formulation and reply-formulation exposure.
- The fixture validates one question interaction and one reply interaction.
- Posts `#4` and `#7` remain untested as quiz steps.
- One selected source sequence can support progressive reveal for the reviewed pair.
- Source-native reply edges, projected tree parents, and selected source-sequence order are distinct relationships.
- Human review remains required before acceptance.

These observations are evidence for later package-boundary work.
They are not yet normative package requirements.
